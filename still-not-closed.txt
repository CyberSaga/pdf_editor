From the canonical tracker `docs/plans/2026-04-09-backlog-execution-order.md`, the phases still not closed are:

- **Phase 6 (`F1`)**: `in_progress`
  - Meaning: “Objects mode” work is still considered incomplete in the tracker (even though we just landed a big slice). Remaining scope is the v2/v3 follow-ups: objects-mode parity, resize handles, multi-select, app-inserted images, and later native PDF images.

- **Phase 8 (`UX7`)**: `open` (closure type is `done-plan`)
  - Meaning: macOS native menu bar is not implemented; it closes once the child plan is reviewed/approved and successor items are tracked.

- **Phase 9 (`F4`, `F2`, `F3`)**: `open` (closure type is `done-plan`)
  - Meaning: these epics don’t close by implementation from the master tracker; they close once their child plans are approved and decomposed into concrete successor items with acceptance criteria.

If you want, I can update Phase 6 (`F1`) status in the tracker to reflect what’s now actually landed vs. what remains.