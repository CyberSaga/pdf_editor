visual_pdf_editor/
├── main.py                 # 入口點，初始化MVC
├── model/
│   └── pdf_model.py        # Model: PDF邏輯
├── view/
│   └── pdf_view.py         # View: UI元件
├── controller/
│   └── pdf_controller.py   # Controller: 事件處理
├── utils/
│   └── helpers.py          # 輔助函數（如顏色選擇、頁碼解析）
└── requirements.txt        # 依賴: PySide6, fitz(PyMuPDF), pytesseract (for OCR)