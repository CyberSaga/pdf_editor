pdf_editor/
  main.py
    # 入口：建立 PDFModel / PDFView / PDFController
    # - 全域 logging.basicConfig 只在這裡初始化；其他模組只拿 named logger

  model/
    pdf_model.py
      # 核心 Model：
      # - 多分頁 session registry（DocumentSession）
      # - fitz.Document 擁有權
      # - 文字編輯交易流程（驗證 / 回滾）
      # - 字型 token 解析與 CJK 字型 CSS/@font-face 組裝（htmlbox 路徑）
      # - 新增文字框（add_text）插入流程
      # - 開檔、存檔、頁面操作
      # - 與 CommandManager、ToolManager 整合
    text_block.py
      # 文字索引：TextBlock / EditableSpan(run) / EditableParagraph(paragraph)
      # - 索引頁面狀態：missing/clean/stale（支援 insert/delete 後 lazy rebuild）
    edit_commands.py
      # Undo/Redo：EditTextCommand / AddTextboxCommand / SnapshotCommand / CommandManager
    tools/
      __init__.py
      base.py
        # ToolExtension 介面（session lifecycle / overlay / save hooks）
      manager.py
        # ToolManager：註冊並管理內建工具（annotation, watermark, search, ocr）
      annotation_tool.py
        # 註解工具（highlight/rect/freetext/顯示切換/保護註解）
      watermark_tool.py
        # 浮水印工具（CRUD、渲染、嵌入與還原 metadata、dirty 狀態）
      search_tool.py
        # 文字搜尋工具
      ocr_tool.py
        # OCR 工具（pytesseract + Pillow；缺依賴時回傳清楚 RuntimeError）

  controller/
    pdf_controller.py
      # Controller：
      # - 接收 View signals
      # - 呼叫 Model 與 model.tools.* API
      # - 維護每個 session 的 UI 狀態（頁碼 / 縮放 / 模式 / 搜尋）
      # - 協調重繪、列印、Undo/Redo

  view/
    pdf_view.py
      # Qt UI：
      # - Tab 文件介面、工具面板、畫布、事件信號
      # - 不直接做資料層業務邏輯
      # - 編輯意圖分流：edit_existing / add_new
      # - Inline editor 焦點守衛：字體/字級 popup 互動不提前 finalize
      # - 文字屬性按鈕：套用(提交) / 取消(捨棄)
      # - 匯出對話框：當前頁/指定頁、DPI、PDF/影像格式一次設定

  src/
    printing/
      # 列印子系統：dialog / dispatcher / page selection / layout / drivers
      # - UnifiedPrintDialog 印表機列新增「屬性」按鈕，可開啟系統原生印表機設定視窗（支援平台）
      # - 屬性視窗關閉後，將驅動回傳的紙張/方向/雙面/色彩/DPI/份數同步回 app 對話框
      # - 紙盤與廠商私有設定不在 app 對話框暴露，直接沿用系統屬性
      # - 紙張/方向/雙面/色彩採 touched precedence：未改動就沿用系統屬性，只有使用者在 app 內改動才覆寫
      # - 若某硬體欄位讀不到系統預設，dialog 退回目前 UI 值並將該欄位視為本次列印明確覆寫
      # - 份數/DPI/逐份列印/頁面範圍/縮放仍屬 app 工作設定，不受系統屬性接管
      # - 不再顯示紙盤或「系統屬性（唯讀）」區塊；紙盤仍以 paper_tray="auto" 方式 pass-through 給系統驅動
      # - Windows 若原生屬性只更新 private DriverExtra 而未更新 public DEVMODE，dialog 將色彩顯示為「依系統屬性」
      # - 預覽可直接透過 live-document provider 單頁渲染，不必在開啟 dialog 前先生成整份列印 snapshot
      # - 真正的 build_print_snapshot / temp PDF 生成延後到使用者按下列印確認之後
      # - preview refresh 有安全包裝；自訂頁碼等暫時無效輸入只會顯示 preview error，不會從 UI event 直接丟例外
      print_dialog.py
      dispatcher.py
      page_selection.py
      layout.py
      pdf_renderer.py
      qt_bridge.py
      errors.py
      base_driver.py
      platforms/
        win_driver.py
        linux_driver.py
        mac_driver.py

  utils/
    helpers.py
      # 共用工具：頁碼解析、pixmap 轉換、錯誤提示等

  docs/
    README.md
    README.zh-TW.md
    ARCHITECTURE.md
    FEATURES.md
    架構.txt
    solutions.md
    Methodology_for_Writing_Docs.md

  test_scripts/
    # 測試與壓力測試腳本（pytest tests + script-style 測試）

目前主要工作流程：
1) 編輯既有文字：
   view(sig_edit_text) -> controller(EditTextCommand) -> model(edit_text)
   - 若編輯框內容清空並提交，視為刪除目標文字框內容（保留 Undo/Redo）

2) 新增頁面文字框：
   view(sig_add_textbox) -> controller(AddTextboxCommand) -> model(add_textbox)

3) 復原 / 重做：
   view -> controller -> command_manager -> command restore -> controller refresh

4) 匯出頁面：
   view(ExportPagesDialog/sig_export_pages) -> controller(export_pages) -> model(export_pages)
   - 影像：scale = dpi / 72、jpg/png 走 Pixmap.save、tiff 走 Pixmap.pil_save(TIFF)
   - 多頁輸出命名：*_p{page_num}

文件分工規則：
- 行為規格：docs/FEATURES.md
- 實作對應：docs/ARCHITECTURE.md
- 問題修正與決策歷史：docs/solutions.md
