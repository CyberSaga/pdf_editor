
● Code Review: Backlog Plans vs. Codebase

  Scope

  Reviewed what's actually shipped against the 8 referenced plans. Only F3 and the native-image follow-up are marked
  done; the others are done-plan awaiting approval. Review focuses on (a) whether "done" claims match code, (b) plan
  quality for the unbuilt ones.

  ---
  F3 Shell-Integration APIs — ✅ Implemented, minor deviations from plan

  What shipped: main.parse_cli, main.run_merge_and_exit, model/headless_merge.py, utils/single_instance.py,
  PDFController.handle_forwarded_cli — all present. Tests listed in the checklist exist.

  Deviations from plan worth flagging:
  - main.py:94 — when forwarding fails, returns exit code 1. Plan says "exit 0". Arguably exit-1 is better (failure
  signal), but the plan's verification step assumes 0. Recommend updating the plan or the code; silent mismatch.
  - main.py:81 uses view._queue_or_open_paths — reaching into a private _ method from main.py violates the underscore
  convention. Promote to a public method on PDFView.
  - utils/single_instance.py:187 — send_to_running_instance short-circuits by calling on_message directly when the
  server lives in the same process (test harness path). This bypasses the JSON/ack round-trip and is a testing back-door
   baked into production code. Gate it behind an explicit test hook or document it clearly — right now it's an invisible
   behavior divergence.
  - _service_local_server manually pumps Qt events from inside the client call. This is correct for the in-process test
  scenario but fragile — a real second-process invocation will never hit this branch. Consider splitting the two code
  paths.
  - headless_merge.py:17-19 raises FileNotFoundError on missing parent directory, matching the plan's "least-surprise"
  choice. Good.
  - QLockFile was added (not in plan) — sensible belt-and-suspenders, worth a line in docs/ARCHITECTURE.md.

  Tracker consistency: checklist, execution-order, and TODOS.md all agree F3 is closed. Good.

  ---
  F2 Surya OCR — Plan only, code not started

  Plan is thorough. Risks to flag before approval:
  - Task 3's render-mode-3 invisible text approach is correct for searchable PDFs but the "pixel hash equal pre/post
  insert within tolerance" test will be flaky — insert_text can subtly shift antialiasing even at mode 3 if the font
  metrics cause any glyph-advance side effects. Prefer asserting no visible pixels changed outside the text bbox, or
  compare via a higher tolerance only inside bboxes.
  - Surya's actual API (DetectionPredictor / RecognitionPredictor) has shifted across recent releases; plan acknowledges
   this ("verify at implementation time") — pin a version in requirements-optional.txt.
  - Per-page commit on cancel (Task 5) is the right call and well-justified.

  ---
  F4 Color Profile Switching — Plan only

  - Task 4's open question #3 ("extend cache key vs. wipe") — strongly prefer key extension. Wiping on every toggle
  defeats the whole point of the B4 campaign's cache work.
  - Task 6: PDFRenderer._pixmap_to_qimage CMYK→RGB path is under-specified. fitz.Pixmap(fitz.csRGB, cmyk_pix) is the
  right bridge; call it out as a required step rather than "may need".
  - View-only scope is well-guarded. Good.

  ---
  UX7 macOS Menu Bar — Plan only

  - Task 3 proposes overwriting shortcuts with QKeySequence.StandardKey only on macOS. Two-path shortcut config is a
  maintenance hazard — simpler to use StandardKey everywhere (Qt already maps correctly per platform) and delete the
  _redo_mac_shortcut hack.
  - Test assertion id(menu_action) == id(view._action_open) is fine but brittle; menu_action is view._action_open is
  idiomatic.
  - Deferred macOS keep-alive decision is correct for this slice.

  ---
  Native-Image Review Follow-Up — Plan only, not yet executed

  Plan is sound. Note: the fragile fzImg0 fixture it calls out is still present in
  test_scripts/test_native_pdf_images_model.py (git status shows the file is new/untracked on this branch, but the plan
  hasn't been executed yet). This should be the next slice before F2/F4/UX7 since it's a known P1 bug in a closed phase.

  ---
  Cross-cutting findings

  - TODOS.md correctly reflects F3 close; no stale entries.
  - Backlog checklist line 82: "Clear legacy ruff debt" still open — consider interleaving with F2/F4 rather than
  letting it bit-rot further.
  - Untracked files visible in git status suggest F3 commit was never actually made (model/headless_merge.py,
  utils/single_instance.py, test_scripts/test_cli_argparse.py, etc. are ??). The tracker claims F3 is done, but the
  evidence isn't committed. Verify with git status and commit before claiming closure — this is a real DoD violation per
   CLAUDE.md §7.

  ---
  Recommended next steps (order)

  1. Commit the F3 slice — untracked files contradict the "done" status.
  2. Execute the native-image follow-up (small, closes a known P1).
  3. Pick one of F2 / F4 / UX7 to approve; F4 is the smallest and lowest-risk.
  4. Fix the main.py:81 private-method reach and decide on the exit-code-1-vs-0 question.

✻ Sautéed for 57s

※ recap: Reviewed the shipped code against the eight backlog plans. F3 is implemented but its files are still untracked
  in git, contradicting its "done" status. Next: commit the F3 slice before moving on to F2, F4, UX7, or the
  native-image follow-up. (disable recaps in /config)


● ✅ F3 Implementation Committed

  All 12 F3 tests pass + 19 startup regression tests pass. Code is ruff-clean.

  Summary of issues flagged in review:

  3 deviations from plan worth addressing before next phase:

  1. Exit code mismatch (minor): Plan says exit 0 on failed forward; code returns 1. Better behavior but contradicts
  verification step. ✅ Acceptable as-is (exit-1 is more correct).
  2. Private method reach (style): main.py:81 uses view._queue_or_open_paths. Violates underscore convention. Action
  needed: Promote to public method on PDFView.
  3. In-process IPC back-door (architectural): utils/single_instance.py:187 bypasses JSON/ack protocol in test harness.
  Action needed: Document clearly or gate behind test hook.

  Would you like me to:
  - Fix the private method issue before final PR?
  - Document the in-process shortcut with a comment?
  - Proceed to review/approve one of the child plans (F2/F4/UX7)?