# 比起「解決問題」、「增加功能」，更優先的是「研究、測試怎麼用好AI」

## 問題修復

* ~~"適應畫面"後縮放的數值需要同步調整成實際數值，以便使用者之後再度調整時縮放比例仍保持準確~~ OK!
* ~~大多數狀況下開啟軟體都只需要 2 秒，但有時開啟需要等到 10 秒~~ OK!
* ~~沒有辦法將文字框內的文字刪到無文字留存~~ OK!
* ~~送出列印後程式會停止回應，直到恢復回應或將程式關掉列印資料才會被送到印表機~~ 一半OK
* ~~列印頁面右上角的亂碼是什麼，是不是影響檢視頁數了~~ OK!

* 將所有工作流程都加速（包含：最佳化檔案速度、開啟檔案後換頁）
-> 查明根本成因並修復

## 做法修改：

* ~~開新檔案時工具列要保持在使用者已經選好的狀態~~ OK!
* ~~選定一項工具（編輯文字、矩形、螢光筆等）就一直保留在該工具，直到使用者改選取別的工具~~ OK!
* ~~開啟編輯框時一旦點選字級就會結束編輯框，需要改善以允許修改字級~~ OK!
* ~~字體也要能套用上去~~ OK!
* ~~縮圖列表每一排要只有一張頁面~~ OK!
* ~~縮圖列表要隨著主畫面滾動到哪裡跟著滾動，讓主畫面焦點的頁數同時也是縮圖列表焦點的頁數~~ OK!
* 縮圖要跟著側邊欄的寬度一起變寬，或是在過寬的側邊欄置中
* 編輯轉向的文字時，文字框裡面的文字也要跟著轉向，而不只是縮窄文字框的寬度
* 編輯文字時，文字框要透明底，顯示字將會被儲存的顏色
* ~~OCR UI 將所有功能整合進同一頁面~~ OK!
* ~~匯出頁 1. 頁數輸入框旁邊顯示總頁數（使用者才知道數字最大可以寫到哪一頁），2. 可以選擇只匯出「當前頁」~~ OK!
* ~~不是「以影像匯出」選是否，而是兩個選項選要用 PDF 還是影像（開啟儲存的對話視窗(Save As Diglog Box)後可以選擇 PNG / JPG ）~~ 匯出 OK!
* ~~每次操作頁面（例如刪除、插入幾頁），就會將整份檔案重新索引一次，檔案大時會很當~~ 改成優先索引改動的頁面，其他推到背景慢慢處理了。但還是很當 give up!
* ~~整縮放比例也是優先縮放當前頁面，其他推到背景慢慢處理 不如說只要檔案大，就算單純瀏覽都會很當~~ OK!
* ~~左右側邊欄要有快捷鍵，可以點快捷鍵把側邊欄關掉/開啟~~ OK!
* 列印要能依頁面方向自動轉向
-> 提出建議新做法並實作

## 功能新增：

* ~~預設在游標移動到文字上時切換成"文字選擇"狀態~~ OK!
* ~~"新增文字"功能~~ OK!
* "新增物件"、"操作物件"功能（包含將文字框轉向）
* ~~快捷鍵：Ctrl + Z 復原, Ctrl + Y 重做, Ctrl S 存檔, Ctrl Shift S 另存新檔, F2 編輯, 提醒存檔時 Y 儲存 N 放棄, Ctrl + O 開啟新檔, Ctrl + P 啟動列印方塊, F5 全螢幕顯示~~ OK! 
* ~~全螢幕顯示~~ OK! 
* OCR 功能引入 [Surya](https://github.com/datalab-to/surya)
* ~~列印：選擇紙盤、紙張大小，或是「直接調用系統對話方塊」~~
* ~~即便直接調用系統對話方塊，還是得在自己的對話框選擇紙盤、紙張大小才能送到印表機~~
* ~~↑ 可以動態讀取所有私有屬性嗎？還是必須先寫好在程式碼裡才讀得到？~~ give up!
* ~~合併 PDF，~~ OK!
* 以及檔案右鍵選單「用 PDF Editor 合併 PDF」
* ~~壓縮 PDF 大小~~
* ~~將 PDF 檔案拖曳到視窗上以在程式中開啟檔案~~ OK!
* 切換色彩描述檔（RGB、CMYK、灰階、黑白?）
-> 提出建議的實現方法並實作