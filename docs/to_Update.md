## 問題修復

* "適應畫面"後縮放的數值需要同步調整成實際數值，以便使用者之後再度調整時縮放比例仍保持準確
* 大多數狀況下開啟軟體都只需要 2 秒，但有時開啟需要等到 10 秒
-> 查明根本成因並修復

## 做法修改：

* ~~開新檔案時工具列要保持在使用者已經選好的狀態~~ OK!
* ~~選定一項工具（編輯文字、矩形、螢光筆等）就一直保留在該工具，直到使用者改選取別的工具~~ OK!
* ~~開啟編輯框時一旦點選字級就會結束編輯框，需要改善以允許修改字級~~
* 字體也要能套用上去
* 縮圖列表每一排要只有一張頁面
* 縮圖列表要隨著主畫面滾動到哪裡跟著滾動，讓主畫面焦點的頁數同時也是縮圖列表焦點的頁數
* 編輯轉向的文字時，文字框裡面的文字也要跟著轉向，而不只是縮窄文字框的寬度
-> 提出建議新做法並實作

## 功能新增：

* ~~預設在游標移動到文字上時切換成"文字選擇"狀態~~ OK!
* ~~"新增文字"功能~~ OK!
* "新增物件"、"操作物件"功能（包含將文字框轉向）
* ~~快捷鍵：Ctrl + Z 復原, Ctrl + Y 重做, Ctrl S 存檔, Ctrl Shift S 另存新檔, F2 編輯, 提醒存檔時 Y 儲存 N 放棄, Ctrl + O 開啟新檔, Ctrl + P 啟動列印方塊~~ OK!
* 全螢幕顯示
-> 提出建議的實現方法並實作