## 問題修復

* 列印完應該不要改動印表機喜好設定才對
* 框選文字要以行為單位，非框選「游標經過的文字」而是框選游標經過的「整行」，並以游標起始的位置和停的位置為框選範圍的前後限
* 編輯轉向的文字時，文字框裡面的文字也要跟著轉向，而不只是縮窄文字框的寬度
* 列印要能依頁面方向自動轉向
* 列印要能依來源頁面選擇紙張大小
* 文件檢視起來還得再更清晰一點（現在文字跟圖片都還是偏模糊）
* 圖片的長寬比會被強制改為選取框的長寬比（1. 剛插入圖片第一次選取時，選取框是正方形，這時候一旦拖動圖片，圖片就會被重製成為正方形；2. 在旋轉圖片的時候，選取框不會跟著旋轉，因此長邊會被縮窄成寬度、寬邊會被拉伸成長度）
* OCR 顯示進度條後到實際開始跑之前有很長的一段時間，CPU 跟 GPU 也都沒有在動，不知道是在等什麼 ← 待查明
* 將所有工作流程都加速（包含：最佳化檔案速度、開啟檔案後換頁、列印送出後卡住電腦的時間）
-> 查明根本成因並修復

## 做法修改：

* 點選旋轉鈕是將物件轉動到 90°、180°、270°、360°
-> 提出建議新做法並實作

## 功能新增：

新增快捷鍵：跳回瀏覽模式
-> 提出建議的實現方法並實作

