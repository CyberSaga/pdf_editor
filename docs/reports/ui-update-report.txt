❯ use @appearance_design\ assets to update this app's UI for best UX by
  @docs/implement-ui-changes-from-vectorized-cake.md

● Done. Here's what I implemented, following the plan and the project's Red-Light-first workflow.

  Summary

  New modules
  - view/theme.py — ALPINE_SNOW token dict (translated from the alpine-snow block of appearance_design/colors.css) plus
  build_qss(), a single global QSS string covering QMainWindow, QGroupBox, QPushButton, QLineEdit/QComboBox, the toolbar
  frame, ribbon tabs, tool buttons (hover/pressed/checked), the canvas QGraphicsView well, the drag-drop affordance,
  and the document tab bar.
  - view/icons.py — ICON_DIR, ACTION_ICON_MAP (31 action labels → PNG filenames), and load_icon() (smooth-scaled QIcon,
  returns a null icon on missing files so toolbars never break).

  view/pdf_view.py changes
  - Removed ~8 scattered inline setStyleSheet blocks (global window, graphics view, document tab bar, toolbar container,
  ribbon tabs, the toolbar_style string) and replaced them with one self.setStyleSheet(build_qss()).
  - Gave the toolbar frame objectName="toolbar" and the document tab bar objectName="documentTabBar" so the global QSS
  scopes them with id selectors (avoids the two tab bars colliding on a bare QTabBar rule).
  - Added _configure_tool_toolbar() (sets ToolButtonTextUnderIcon + 24px icon size) and _apply_toolbar_icons() (assigns
  icons by action.text()); both applied to all 5 toolbars. "操作物件" gets its icon in both tabs automatically.
  - Added _configure_tool_toolbar() (sets ToolButtonTextUnderIcon + 24px icon size) and _apply_toolbar_icons() (assigns
  icons by action.text()); both applied to all 5 toolbars. "操作物件" gets its icon in both tabs automatically.
  - Retuned the default rect/highlight swatches to #4338ca / #f0d670, keeping the QColor value and button stylesheet in
  sync so the swatch stays truthful.

  Tests / docs
  - test_scripts/test_theme_and_icons.py — 6 tests (confirmed Red before the modules existed, now Green): tokens, QSS
  content/selectors, icon-file existence, mapping coverage, load_icon behavior incl. the missing-file path.
  - Updated docs/ARCHITECTURE.md (new §2.4.1), docs/PITFALLS.md (QTabBar selector collision), and TODOS.md.

  Verification: ruff check view/ test_scripts/test_theme_and_icons.py clean; new tests pass; targeted GUI regression
  suites (interaction modes, color profile, multi-tab, menu, OCR, fullscreen, dialogs, object/text GUI — 186 tests) all
  pass; a headless smoke instantiation confirms 5 toolbar tabs, non-null action icons, and the QSS applied.

  One deviation from the plan worth flagging: the plan listed only the button stylesheet color swaps, but those swatches
  mirror the actual default annotation colors — I changed the QColor defaults too so the swatch can't lie. This does
  shift the default rectangle color to indigo and highlight to the warm yellow token. No tests depended on the old
  defaults. Let me know if you'd prefer the default annotation colors left unchanged.

※ recap: Goal: apply the Alpine Snow theme and toolbar icons to the PDF editor UI. This is done and verified, with new
  modules, tests passing, and docs updated. Next: optionally re-run the codegraph indexer for the two new modules.
  (disable recaps in /config)

❯ /codex:review all toolbuttons are expected to have texts besides icon, right?

● This is a moderately sized change (4 modified files plus 4 untracked, including two new source modules and a test
  suite). I'll recommend running in the background.

● Codex review complete. The focus text (all toolbuttons are expected to have texts besides icon, right?) isn't
  supported by the built-in reviewer, so I ran the native working-tree review. Output verbatim:

  ---
  Codex Review

  Target: working tree diff

  The patch introduces UI regressions in the main toolbar layout and applies ribbon tab styling to unrelated sidebar
  tabs. These are functional presentation issues that should be fixed before considering the change correct.

  Full review comments:

  - [P1] Increase toolbar height for icon-over-text buttons — C:\Users\jiang\Documents\python
  programs\pdf_editor\view\pdf_view.py:971-972
  When the app starts with the new ToolButtonTextUnderIcon/24px icon layout, each toolbar button needs icon + label
  height, but _toolbar_container is still fixed at 60px while also containing the tab bar and vertical margins. This
  leaves less than the toolbar's required height, so the primary ribbon buttons will be clipped unless the container
  height/layout is adjusted.
  - [P2] Scope ribbon tab styling away from sidebar tabs — C:\Users\jiang\Documents\python
  programs\pdf_editor\view\theme.py:67-75
  Because build_qss() is applied to the whole PDFView, these unscoped QTabWidget/QTabBar rules also apply to
  self.left_sidebar, not just the ribbon tabs. In normal use this restyles the thumbnail/search/annotation sidebar tabs
  with ribbon sizing and selected colors, so the ribbon rules should be scoped with an object name the same way the
  document tab bar is.

  ---
  Regarding your question — yes, both findings are consistent with the intent that all toolbuttons keep text alongside
  (under) the icon: P1 flags that the fixed 60px container won't fit icon+text and will clip the labels, and P2 flags
  ribbon tab styling leaking onto the sidebar tabs. I haven't made any changes (review-only). Let me know if you'd like
  me to address either finding.

※ recap: Goal: apply the Alpine Snow theme and toolbar icons to the PDF editor. The work is implemented and tested, but
  a Codex review flagged two issues. Next: fix the 60px toolbar clipping and scope ribbon QSS off the sidebar tabs.
  (disable recaps in /config)

❯ for each toolbutton, show label texts at right side of icon

● Done. Each toolbutton now shows its label text to the right of the icon.

  Change: _configure_tool_toolbar() in view/pdf_view.py now sets Qt.ToolButtonTextBesideIcon (was
  ToolButtonTextUnderIcon), keeping the 24px icon size. Updated the matching docstring and docs/ARCHITECTURE.md.

  A side benefit: this also resolves the Codex P1 finding — text-beside-icon needs less vertical room than
  text-under-icon, so the fixed 60px toolbar container no longer risks clipping the labels.

  Verified: ruff clean, all 5 ribbon toolbars report ToolButtonTextBesideIcon, theme + interaction-mode tests pass.

  Note: the Codex P2 finding (ribbon QTabBar/QTabWidget QSS leaking onto the left sidebar's thumbnail/search/annotation
  tabs) is still open — I didn't touch it since you only asked for the label layout. Want me to scope those rules with
  an object name too?

※ recap: Goal: apply the Alpine Snow theme and toolbar icons to the PDF editor. Toolbuttons now show text beside icon
  and tests pass. Next: optionally fix the Codex P2 finding where ribbon tab styling leaks onto the left sidebar tabs.
  (disable recaps in /config)

❯ commit changes and then fix P2

● I'll commit the current work first, then fix P2.

● Both done.

  Commit 1 — 177c1b6 feat(ui): apply Alpine Snow theme and wire toolbar icons — the theme + icons work (with the
  text-beside-icon button layout), clean message.

  Commit 2 — 031b8b9 fix(ui): scope ribbon tab QSS so it doesn't leak onto sidebar tabs — the Codex P2 fix.

  P2 fix details

  - The unscoped QTabWidget/QTabBar rules in build_qss() were matching every tab widget in PDFView, including the left
  sidebar (縮圖 / 搜尋 / 註解列表 / 浮水印列表).
  - Gave the ribbon QTabWidget objectName("ribbonTabs") and rewrote its rules as QTabWidget#ribbonTabs::pane /
  QTabWidget#ribbonTabs QTabBar::tab. The sidebar QTabWidget is deliberately left unstyled, so it keeps the native look.
  - Added test_tab_rules_are_object_scoped which fails if any unscoped tab rule reappears, and updated the PITFALLS
  entry.

❯ add other 2 color mode in @appearance_design/colors.css , allow user to switch between
  ⎿  Read appearance_design\colors.css (218 lines)

● User answered Claude's questions:
  ⎿  · What should the 2 new color modes look like? The plan filename hints at "Glimmering Glacier" — is that one of them? →
     Glimmering Glacier + Amber Night (Recommended)
     · Where should the theme switcher appear in the UI? → 3 small squares using color that can represent each. squares
     always show on app's corner, and shows name when hover on it.

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 Ready to code?

 Here is Claude's plan:
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
 Plan: Add Glimmering Glacier + Amber Night Themes with Theme Switcher

 Context

 The app has 3 themes fully designed in appearance_design/colors.css, but only Alpine Snow is wired in Python. No
 theme-switching UI exists. This plan adds 2 new themes (Glimmering Glacier — light icy cerulean; Amber Night — dark warm
 amber) and a persistent theme-switcher widget: a row of 3-swatch color chips in the status bar corner, always visible,
 tooltip on hover, accent ring on active chip.

 New Theme Token Values

 Glimmering Glacier (glimmering-glacier) — light, cerulean accent

 GLIMMERING_GLACIER: dict[str, str] = {
     "bg": "#e8f4fb",  "surface": "#f4fafd",  "sunken": "#d6ecf5",  "elev": "#ffffff",
     "line": "#b8d9ee",  "line_strong": "#7db9d8",  "line_soft": "#daeef7",
     "fg": "#0c1a2e",  "fg_muted": "#2d4a64",  "fg_subtle": "#5d7a8e",
     "accent": "#0369a1",  "accent_fg": "#ffffff",  "accent_soft": "#cce6f7",
     "hover": "rgba(3,105,161,0.06)",  "pressed": "rgba(3,105,161,0.14)",
     "highlight": "#67e8f9",  "paper": "#ffffff",
 }

 Amber Night (amber-night) — dark, warm amber

 AMBER_NIGHT: dict[str, str] = {
     "bg": "#1c1810",  "surface": "#252018",  "sunken": "#130f0a",  "elev": "#2f2820",
     "line": "#3d3428",  "line_strong": "#5a4d38",  "line_soft": "#2a2318",
     "fg": "#f5ead8",  "fg_muted": "#c2a87a",  "fg_subtle": "#8a7355",
     "accent": "#d97706",  "accent_fg": "#1c1810",  "accent_soft": "rgba(217,119,6,0.15)",
     "hover": "rgba(217,119,6,0.07)",  "pressed": "rgba(217,119,6,0.16)",
     "highlight": "#fbbf24",  "paper": "#f7f0e3",
 }

 Files to Change

 1. appearance_design/colors.css

 Append 2 complete CSS blocks (blocks 03 and 04) using the same format as the existing blocks. Include all --color-*,
 --shadow-*, and --swatch-* custom properties.

 2. view/theme.py (primary change)

 Add (after ALPINE_SNOW):
 - GLIMMERING_GLACIER and AMBER_NIGHT token dicts (see above)
 - ThemeMeta frozen dataclass: id: str, label: str, tokens: dict[str, str], swatches: tuple[str, str, str]
 - THEME_REGISTRY: dict[str, ThemeMeta] for all 3 themes
 - _DEFAULT_THEME = "alpine-snow"

 Modify build_qss():
 - Change signature to build_qss(theme_name: str = _DEFAULT_THEME) -> str
 - Replace t = ALPINE_SNOW with t = (THEME_REGISTRY.get(theme_name) or THEME_REGISTRY[_DEFAULT_THEME]).tokens
 - QSS f-string body is unchanged

 Add two new classes at bottom:

 _ThemeChip(QFrame) — draws 3 colored squares via paintEvent + accent ring when active:
 - clicked = Signal()
 - set_active(bool) → calls self.update()
 - paintEvent: draw 3 SIZE=14px rounded squares from meta.swatches; if active, draw 2px ring using meta.tokens["accent"]
 - mousePressEvent: emit clicked on left button

 ThemeSwitcherWidget(QWidget) — row of chips:
 - theme_selected = Signal(str)
 - Creates one _ThemeChip per registry entry, connects chip.clicked → self.theme_selected.emit(theme_id)
 - set_active_theme(theme_id: str) → updates each chip's active state

 Qt imports: wrap in try/except ImportError at module level so token-only tests don't need a QApplication.

 3. utils/preferences.py

 Add constants and methods to UserPreferences:
 _THEME_KEY = "ui/theme"
 _THEME_DEFAULT = "alpine-snow"
 _VALID_THEME_IDS = frozenset({"alpine-snow", "glimmering-glacier", "amber-night"})

 def get_theme(self) -> str:  # validates; falls back to default if unknown
 def set_theme(self, name: str) -> None:  # raises ValueError for unknown names
 Keep _VALID_THEME_IDS as a frozenset constant (avoids importing view.theme into utils/, which would be a layer violation).

 4. view/pdf_view.py

 - Update import: from view.theme import build_qss, ThemeSwitcherWidget
 - Add from utils.preferences import UserPreferences
 - Add signal: sig_theme_selected = Signal(str) (near other signals ~line 284)
 - In __init__ near line 556:
 self._prefs = UserPreferences()
 initial_theme = self._prefs.get_theme()
 self.setStyleSheet(build_qss(initial_theme))
 self._theme_switcher = ThemeSwitcherWidget(active_theme=initial_theme, parent=self)
 self.statusBar().addPermanentWidget(self._theme_switcher)
 self._theme_switcher.theme_selected.connect(self._on_theme_selected)
 - Add slot: _on_theme_selected(self, theme_id: str) → emits self.sig_theme_selected
 - Add method: update_theme_switcher(self, theme_id: str) → calls self._theme_switcher.set_active_theme(theme_id)

 5. controller/pdf_controller.py

 - In _connect_signals: self.view.sig_theme_selected.connect(self.set_theme)
 - Add slot:
 @Slot(str)
 def set_theme(self, name: str) -> None:
     from utils.preferences import UserPreferences
     from view.theme import build_qss, THEME_REGISTRY
     if name not in THEME_REGISTRY:
         return
     UserPreferences().set_theme(name)
     self.view.setStyleSheet(build_qss(name))
     self.view.update_theme_switcher(name)

 6. test_scripts/test_theme_and_icons.py + test_user_preferences.py

 Write all tests before implementation (red-green per CLAUDE.md):

 Token/registry tests (no Qt needed):
 - test_glimmering_glacier_tokens_complete — all 17 keys present, accent == #0369a1
 - test_amber_night_tokens_complete — all 17 keys, accent == #d97706, paper == #f7f0e3
 - test_theme_registry_has_three_entries
 - test_theme_meta_swatches_match_tokens — swatches[0/1/2] == tokens[bg/surface/accent]

 build_qss tests (no Qt):
 - test_build_qss_glimmering_glacier — contains #0369a1 and #d6ecf5
 - test_build_qss_amber_night — contains #d97706 and #130f0a
 - test_build_qss_unknown_falls_back — unknown name returns Alpine Snow QSS
 - test_build_qss_default_is_alpine_snow — build_qss() == build_qss("alpine-snow")

 Preferences tests (add to test_user_preferences.py):
 - Default → "alpine-snow"; round-trip set/get; shared store persists across instances; unknown name raises ValueError;
 corrupt stored value falls back to default

 Widget tests (need qapp fixture):
 - Chip count matches registry; active chip set on init; theme_selected signal emits on click; set_active_theme updates
 chip ring

 Execution Order (red-green)

 1. Write all tests → run pytest → confirm red
 2. view/theme.py: add token dicts, ThemeMeta, registry, parameterize build_qss → token/build tests go green
 3. utils/preferences.py: add theme methods → preferences tests go green
 4. view/theme.py: add _ThemeChip + ThemeSwitcherWidget → widget tests go green
 5. view/pdf_view.py: wire startup + signal + switcher
 6. controller/pdf_controller.py: connect signal + add slot
 7. appearance_design/colors.css: append 2 CSS blocks
 8. ruff check . zero violations; pytest full pass
     │ 3. utils/preferences.py: add theme methods → preferences tests go green   │
     │ 4. view/theme.py: add _ThemeChip + ThemeSwitcherWidget → widget tests go green   │
     │ 5. view/pdf_view.py: wire startup + signal + switcher   │
     │ 6. controller/pdf_controller.py: connect signal + add slot   │
     │ 7. appearance_design/colors.css: append 2 CSS blocks   │
     │ 8. ruff check . zero violations; pytest full pass   │
     │   │
     │ Known Gotchas   │
     │   │
     │ - accent_soft in Amber Night is rgba(...) — QColor only parses hex. The chip paintEvent uses tokens["accent"]   │
     │ (always hex) for the ring, so this is safe.   │
     │ - _VALID_THEME_IDS in preferences must stay in sync with THEME_REGISTRY manually — add a comment noting the sync   │
     │ requirement.   │
     │ 6. controller/pdf_controller.py: connect signal + add slot   │
     │ 7. appearance_design/colors.css: append 2 CSS blocks   │
     │ 8. ruff check . zero violations; pytest full pass   │
     │   │
     │ Known Gotchas   │
     │   │
     │ - accent_soft in Amber Night is rgba(...) — QColor only parses hex. The chip paintEvent uses tokens["accent"]   │
     │ (always hex) for the ring, so this is safe.   │
     │ - _VALID_THEME_IDS in preferences must stay in sync with THEME_REGISTRY manually — add a comment noting the sync   │
     │ requirement.   │
     │ - pdf_view.py uses self.resizeEvent = self._resize_event (monkey-patch). The ThemeSwitcherWidget lives in the   │
     │ QStatusBar (not an overlay), so no resize handling is needed.   │

❯ implement @docs/plans/add-other-2-color-glimmering-glacier.md
  ⎿  Read docs\plans\add-other-2-color-glimmering-glacier.md (152 lines)

● Implementation complete. Here's a summary of what was done, following the red-green TDD workflow from CLAUDE.md.

  What changed

  Tests first (red → green): Added 21 tests across test_theme_and_icons.py (token completeness, registry, build_qss
  fall-back, _ThemeChip/ThemeSwitcherWidget behavior) and test_user_preferences.py (theme round-trip, corrupt-value
  recovery, unknown-name ValueError). Confirmed all 21 failed before implementing.

  view/theme.py — GLIMMERING_GLACIER + AMBER_NIGHT token dicts, ThemeMeta frozen dataclass, THEME_REGISTRY, _DEFAULT_THEME;
  parameterized build_qss(theme_name) with fall-back to Alpine Snow; _ThemeChip + ThemeSwitcherWidget. Qt imports wrapped in
  try/except ImportError so token-only tests run headless.

  utils/preferences.py — get_theme() / set_theme() with a local _VALID_THEME_IDS frozenset (kept out of view to avoid a
  utils → view layer violation).

  view/pdf_view.py — sig_theme_selected; startup loads persisted theme and applies build_qss(initial_theme); switcher added
  via statusBar().addPermanentWidget(...); _on_theme_selected + update_theme_switcher.

  controller/pdf_controller.py — set_theme slot wired in _connect_signals (validates, persists, restyles, updates chips).
  Flow stays within layer rules: View emits → Controller mutates → persists.

  appearance_design/colors.css — appended blocks 03 (glimmering-glacier) and 04 (amber-night).

  Verification

  - Full suite: 1105 passed, 22 skipped (129s).

  appearance_design/colors.css — appended blocks 03 (glimmering-glacier) and 04 (amber-night).

  Verification

  - Full suite: 1105 passed, 22 skipped (129s).
  - Ruff: 243 errors before and after — zero new violations (new files are individually clean; the 243 are pre-existing
  tracked violations).
  - Docs updated per post-task protocol: ARCHITECTURE.md (theming section + switch flow), TODOS.md, PITFALLS.md
  (QColor/rgba + layer-boundary gotcha). Plan moved to docs/plans/archive/.

  One note: the plan's preamble said the 3 themes were already designed in colors.css, but the existing blocks there
  were Alpine Snow / Meadow Lupine / Ink Porcelain — so Glimmering Glacier and Amber Night are genuinely new CSS blocks,
  which I authored to match the existing block format and the plan's token values. The two rgba() tokens in Amber Night
  appearance_design/colors.css — appended blocks 03 (glimmering-glacier) and 04 (amber-night).

  Verification

  - Full suite: 1105 passed, 22 skipped (129s).
  - Ruff: 243 errors before and after — zero new violations (new files are individually clean; the 243 are pre-existing
  tracked violations).
  - Docs updated per post-task protocol: ARCHITECTURE.md (theming section + switch flow), TODOS.md, PITFALLS.md (QColor/rgba
  + layer-boundary gotcha). Plan moved to docs/plans/archive/.

  One note: the plan's preamble said the 3 themes were already designed in colors.css, but the existing blocks there were
  Alpine Snow / Meadow Lupine / Ink Porcelain — so Glimmering Glacier and Amber Night are genuinely new CSS blocks, which I
  authored to match the existing block format and the plan's token values. The two rgba() tokens in Amber Night
  (accent_soft) are handled safely: the chip ring uses tokens["accent"] (always hex), as the plan's gotcha noted.

  To eyeball it live: python main.py — three swatch chips appear bottom-right; clicking each recolors the app; the active
  chip gets an accent ring; restart restores the last selection.

※ recap: Added two new themes (Glimmering Glacier, Amber Night) plus a status-bar theme switcher, following TDD. All done:
  1105 tests pass, zero new lint, docs updated, plan archived. Next, optionally run python main.py to eyeball the chips
  live.

❯ /codex:adversarial-review alpine snow should look like @"Alpine Snow.png" , low visual burden should look like @"Meadow
Lupine.png" , dark mode should look like @"Ink Porcelain.png" . also, the switch squares should be only one for each mode.

● Codex Adversarial Review

  Target: working tree diff
  Verdict: needs-attention

  No-ship: the diff wires the wrong selectable themes and the switcher still renders three swatches per mode, directly
  missing the requested visual behavior.

  Findings:
  - [high] Requested Meadow Lupine and Ink Porcelain modes are not selectable (view/theme.py:98-104)
  The registry exposes only alpine-snow, glimmering-glacier, and amber-night, while the CSS already defines the requested
  low-burden meadow-lupine and dark ink-porcelain palettes. The controller and preferences validate against this registry/ID
  set, so users cannot select the themes that should match Meadow Lupine.png and Ink Porcelain.png; they get unrelated blue
  and amber palettes instead.
  The registry exposes only alpine-snow, glimmering-glacier, and amber-night, while the CSS already defines the requested
  low-burden meadow-lupine and dark ink-porcelain palettes. The controller and preferences validate against this registry/ID
  set, so users cannot select the themes that should match Meadow Lupine.png and Ink Porcelain.png; they get unrelated blue
  and amber palettes instead.
  Recommendation: Replace GLIMMERING_GLACIER/AMBER_NIGHT with token dicts for MEADOW_LUPINE and INK_PORCELAIN, registerIDs
  meadow-lupine and ink-porcelain, and update preferences/tests to validate those exact three modes.
  - [medium] Theme switcher renders nine color squares instead of one square per mode (view/theme.py:232-251)
  Each _ThemeChip sizes itself for three squares and then loops over self._meta.swatches, drawing three colored squaresper
  theme. With three modes, the status bar presents nine squares, which violates the requested single switch square per mode
  and increases the visual burden in the exact control the change is meant to simplify.
  Recommendation: Change each chip to draw one square for its mode, using a single representative color per theme, and
  update the widget test to assert one square/chip rather than three swatches.

❯ @Alpine Snow.png @Meadow Lupine.png @Ink Porcelain.png learn from the review findings and commit messages, change colors in
  @docs\plans\add-other-2-color-glimmering-glacier.md , and restart from commit
  24c9dba9ae4b87d6de1eaa1867d7ac6088023f27
    Findings:
    The patch introduces UI regressions in the main toolbar layout and applies ribbon tab styling to unrelated sidebar
    tabs. These are functional presentation issues that should be fixed before considering the change correct.

    Full review comments:

    - [P1] Increase toolbar height for icon-over-text buttons — C:\Users\jiang\Documents\python
    programs\pdf_editor\view\pdf_view.py:971-972
    When the app starts with the new ToolButtonTextUnderIcon/24px icon layout, each toolbar button needs icon + label
    height, but _toolbar_container is still fixed at 60px while also containing the tab bar and vertical margins. This
    leaves less than the toolbar's required height, so the primary ribbon buttons will be clipped unless the container
    height/layout is adjusted.
    - [P2] Scope ribbon tab styling away from sidebar tabs — C:\Users\jiang\Documents\python
    programs\pdf_editor\view\theme.py:67-75
    Because build_qss() is applied to the whole PDFView, these unscoped QTabWidget/QTabBar rules also apply to
    self.left_sidebar, not just the ribbon tabs. In normal use this restyles the thumbnail/search/annotation sidebar
  tabs
    with ribbon sizing and selected colors, so the ribbon rules should be scoped with an object name the same way the
    document tab bar is.

    ---
    Regarding your question — yes, both findings are consistent with the intent that all toolbuttons keep text
  alongside
    (under) the icon: P1 flags that the fixed 60px container won't fit icon+text and will clip the labels, and P2 flags
    ribbon tab styling leaking onto the sidebar tabs. I haven't made any changes (review-only). Let me know if you'd
  like
    me to address either finding.



    Findings:
    - [high] Requested Meadow Lupine and Ink Porcelain modes are not selectable (view/theme.py:98-104)
    The registry exposes only alpine-snow, glimmering-glacier, and amber-night, while the CSS already defines the
  requested
    low-burden meadow-lupine and dark ink-porcelain palettes. The controller and preferences validate against this
  registry/ID
    set, so users cannot select the themes that should match Meadow Lupine.png and Ink Porcelain.png; they get
  unrelated blue
    and amber palettes instead.
    The registry exposes only alpine-snow, glimmering-glacier, and amber-night, while the CSS already defines the
  requested
    low-burden meadow-lupine and dark ink-porcelain palettes. The controller and preferences validate against this
  registry/ID
    set, so users cannot select the themes that should match Meadow Lupine.png and Ink Porcelain.png; they get
  unrelated blue
    and amber palettes instead.
    Recommendation: Replace GLIMMERING_GLACIER/AMBER_NIGHT with token dicts for MEADOW_LUPINE and INK_PORCELAIN,
  register IDs
    meadow-lupine and ink-porcelain, and update preferences/tests to validate those exact three modes.
    - [medium] Theme switcher renders nine color squares instead of one square per mode (view/theme.py:232-251)
    Each _ThemeChip sizes itself for three squares and then loops over self._meta.swatches, drawing three colored
  squares per
    theme. With three modes, the status bar presents nine squares, which violates the requested single switch square
  per mode
    and increases the visual burden in the exact control the change is meant to simplify.
    Recommendation: Change each chip to draw one square for its mode, using a single representative color per theme,
  and
    update the widget test to assert one square/chip rather than three swatches.

    Next steps:
    - Use the existing meadow-lupine and ink-porcelain CSS blocks as the source of truth for Python tokens.
    - Add a regression test that the registry IDs are exactly alpine-snow, meadow-lupine, and ink-porcelain, and that
  each
    chip paints a single square.

  ※ recap: Goal: add theme-switching to the PDF editor. Codex review flagged that the implemented themes (Glimmering
  Glacier,
    Amber Night) don't match your requested Meadow Lupine and Ink Porcelain, and each switcher chip shows three squares
    instead of one. Next: rework to those two themes with single-square chips.

● Done. Here's what happened.

  Summary

  1. Corrected the plan (docs/plans/.../add-other-2-color-glimmering-glacier.md, now archived): swapped the two wrong
  palettes (Glimmering Glacier + Amber Night) for the ones you actually requested — Meadow Lupine (light, low-burden)
  and Ink Porcelain (dark, low-burden) — with exact tokens pulled from colors.css, plus the single-square chip design
  and the P1/P2 review fixes baked into the spec.

  2. Restarted from 24c9dba (safely): committed everything (the discarded commits and uncommitted work) to
  backup/restart-snapshot first, then hard-reset update-ui to 24c9dba, restoring the corrected plan + reference PNGs.
  Nothing is lost — the old attempt is fully recoverable on the backup branch.

  3. Rebuilt the feature red-green, learning from the four findings:

  ┌────────────────────────────┬────────────────────────────────────────────────────────────────────────────────────┐
  │          Finding           │                                        Fix                                         │
  ├────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
  │ Requested palettes not     │ Registry is now exactly alpine-snow, meadow-lupine, ink-porcelain (matches         │
  │ selectable                 │ colors.css; glacier/amber gone)                                                    │
  ├────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
  │ Nine squares in switcher   │ Each chip now draws one representative square (bg) + accent ring when active       │
  ├────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
  │ P1 toolbar clipped         │ _toolbar_container raised 60px → 92px; buttons use text-under-icon + 24px icons    │
  ├────────────────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
  │ P2 ribbon QSS leaks to     │ All ribbon rules scoped to QTabWidget#ribbonTabs; sidebar untouched                │
  │ sidebar                    │                                                                                    │
  └────────────────────────────┴────────────────────────────────────────────────────────────────────────────────────┘

  Files: view/theme.py, view/icons.py, utils/preferences.py, controller/pdf_controller.py, view/pdf_view.py, plus tests
  and docs (ARCHITECTURE §16, two PITFALLS entries, TODOS).

  Verification: new tests went red (no view.theme) → green; 1107 passed / 22 skipped full suite, no regressions; ruff
  check zero new violations (controller's 15 are pre-existing); headless smoke test confirms 92px container, scoped
  ribbon, three correct chips, default theme.

  One thing for you to decide

  update-ui is currently at 24c9dba with the rebuilt feature uncommitted in the working tree. I followed the "commit
  only when asked" rule and stopped short of committing so you can review first. Want me to commit it as a clean feature
  commit on update-ui (and, if so, delete the backup/restart-snapshot branch once you're satisfied)?

  Note: your saved QSettings still holds the old glimmering-glacier value from the discarded run — the code correctly
  ignores it and falls back to Alpine Snow, and it'll be overwritten the first time you click a chip.

✻ Cogitated for 25m 39s

※ recap: Goal: rebuild the theme feature on branch update-ui with the correct Meadow Lupine and Ink Porcelain palettes
  after restarting from commit 24c9dba. Done and tests pass, left uncommitted for review. Next: commit it if you
  approve.

❯ /codex:review

● Codex Review

  Target: working tree diff

  The new theme feature is not correctly applied: startup overwrites the saved theme, major widgets remain hardcoded to
  the old light palette, and the dark theme lacks foreground colors for common controls.

  Full review comments:

  - [P2] Don't overwrite the saved theme during startup — C:\Users\jiang\Documents\python
  programs\pdf_editor\view\pdf_view.py:309-309
  When a user has ui/theme set to meadow-lupine or ink-porcelain, this applies the saved QSS but initialization later
  calls the old hardcoded self.setStyleSheet(...) block in PDFView, replacing it with the previous light palette before
  the window is shown. The switcher can show the saved theme as active while the app starts in the old colors until the
  user clicks a chip.
  - [P2] Let themed rules reach the canvas and document tabs — C:\Users\jiang\Documents\python
  programs\pdf_editor\view\theme.py:176-186
  These theme rules do not actually affect the canvas/document tabs after a theme switch: self.graphics_view still has
  its own hardcoded light stylesheet, and self.document_tab_bar still has a local light stylesheet and never gets object
  name documentTabBar. Because widget-level QSS wins over inherited main-window QSS and the selector misses, selecting
  the dark theme leaves those major surfaces in the old light palette.
  - [P2] Set text colors for the dark theme — C:\Users\jiang\Documents\python programs\pdf_editor\view\theme.py:120-139
  When ink-porcelain is selected on a normal light OS palette, the QSS changes window/surface backgrounds to dark colors
  but never sets a default foreground for common text-bearing widgets such as QLabel, QGroupBox, QPushButton,
  QLineEdit, and QComboBox. Those controls keep the platform's dark text, making labels and controls on the dark chrome
  hard to read.

✻ Sautéed for 4m 39s


● Codex Adversarial Review

  Target: working tree diff
  Verdict: needs-attention

  No ship: the focused UI gap is still present. The theme QSS scopes the ribbon fix but does not add replacement rules
  for the left sidebar or document workspace backgrounds.

  Findings:
  - [medium] Left sidebar and document containers can remain on the default palette (view/theme.py:151-208)
  build_qss() now scopes tab styling to QTabWidget#ribbonTabs, but there is no corresponding themed selector for the
  left sidebar QTabWidget/pane/content or the document workspace container. The only document-area background rule here
  is QGraphicsView, and the document tab styling is limited to QTabBar#documentTabBar; parent panes and sidebar tabs can
  therefore keep Qt/default or previously hardcoded light backgrounds, especially visible under ink-porcelain. This
  directly leaves the requested 縮圖/搜尋 sidebar and docs background visually uncoordinated.
  Recommendation: Assign stable object names to the left sidebar tab widget/panes and the document workspace/tab widget,
  then add explicit QSS rules using the active theme tokens for their backgrounds, panes, tabs, and common child
  views/lists. Add a regression test that build_qss() contains those scoped selectors for all themes.

  Next steps:
  - Add scoped sidebar/document background rules instead of relying on unscoped QTabWidget leakage.
  - Add tests covering the focused surfaces: sidebar selectors and document workspace selectors are present in themed
  QSS.

❯ fix the findings from both reviews

● All findings from both reviews are fixed and verified. Here's the rundown.

  Codex review (3 findings) — all fixed

  1. Startup overwrote the saved theme (pdf_view.py) — There was a second hardcoded self.setStyleSheet("QMainWindow {
  background: #F8FAFC … }") block at the end of __init__ that clobbered the themed QSS applied at the top. Removed it.
  Theme QSS is now applied exactly once. Guard test asserts view.styleSheet() == build_qss(view._initial_theme) and that
  #F8FAFC is gone.
  2. Canvas & document tabs stayed light — graphics_view and document_tab_bar had their own hardcoded light stylesheets
  (widget-level QSS wins over inherited), and the tab bar never got the documentTabBar object name. Removed both inline
  stylesheets; set the object name so the global QGraphicsView / QTabBar#documentTabBar rules apply.
  3. Dark theme had no text colors — QSS set backgrounds but not foreground, so dark chrome kept the OS's dark text.
  Added explicit color: {fg} defaults (base QWidget +
  QLabel/QGroupBox/QPushButton/QLineEdit/QComboBox/QListWidget/QToolButton).

  Adversarial review (your focus) — fixed

  Left sidebar (縮圖/搜尋…) and document area now coordinate with the theme. Gave them stable object names (sidebarTabs,
  leftPanel, rightPanel, colorProfileCard) and added scoped, token-driven build_qss rules for the sidebar tab
  widget/pane/tabs, the panel containers, and QListWidget (with themed selection). Removed the colorProfileCard
  hardcoded border. A parametrized regression test asserts all these selectors exist for every theme.

  Verified via headless smoke: switching to ink-porcelain now carries the dark surface (#232228) and dark foreground
  (#e8e7eb) to the sidebar/panels; no widget retains an inline override.

  Verification

  - 1114 passed, 22 skipped (full suite; +7 new theme guard tests, no regressions)
  - ruff check clean on all edited files (zero new violations)
  - Remaining inline setStyleSheet calls are the intentional data-driven ones only (color-picker swatches, toast,
  fullscreen-exit overlay) — as the plan specified.

  Docs updated: ARCHITECTURE §16 (single-source QSS + object-name scoping), PITFALLS (3 new entries), TODOS (second-pass
  fixes).

  The work is still uncommitted on update-ui (at 24c9dba), with the old attempt preserved on backup/restart-snapshot.
  Say the word and I'll commit it as a clean feature commit.

※ recap: Goal: rebuild the theme switcher (Alpine Snow, Meadow Lupine, Ink Porcelain) cleanly from commit 24c9dba. All
  review findings are fixed and tests pass (1114), with work uncommitted on update-ui. Next: commit it as a clean
  feature commit when you confirm.

❯ /codex:review

● Codex Review

  Target: working tree diff

  The dark theme applies light foreground colors globally while leaving some native control backgrounds light, making
  visible UI controls unreadable in the new theme. This is a functional regression in the introduced theme feature.

  Review comment:

  - [P2] Theme backgrounds before applying dark foreground — C:\Users\jiang\Documents\python
  programs\pdf_editor\view\theme.py:124-126
  When ink-porcelain is selected on a light OS palette, this globally changes widget text to the light dark-theme
  foreground, but several native widgets still keep their default light background because build_qss() does not theme
  them (for example QStatusBar and the quick QPushButtons remain #efefef). That makes status/quick-button text nearly
  unreadable; either add themed backgrounds for those controls or avoid applying the dark foreground to unthemed
  widgets.

❯ execute surgical fixes for finding

● Surgical fix: the global QWidget { color: fg } darkens text everywhere, but a couple of native controls (QPushButton,
  QStatusBar) keep their light default background, so their text becomes unreadable under the dark theme. I'll give
  those two a themed background so background+foreground stay consistent.

● Fixed. 1117 passed, 22 skipped — no regressions (+3 from the new parametrized test).

  What the fix does

  The finding: my global QWidget { color: {fg} } correctly darkened text everywhere, but a few native controls
  (QPushButton, QStatusBar) had no themed background, so they kept the native light grey — light/dark text on a light
  background became unreadable under ink-porcelain.

  Surgical change in view/theme.py build_qss() — pair the foreground with a themed background for exactly those
  controls:

  - QPushButton → background: elev + 1px solid line border, plus :hover (surface), :pressed (sunken), and :disabled
  (fg_subtle) states.
  - QStatusBar → background: surface; color: fg (this is also where the theme switcher lives), with QStatusBar::item {
  border: none }.

  The data-driven color-picker buttons (rect_color_btn, highlight_color_btn) still override with their own inline
  background-color as before — intended.

  Verification

  - New parametrized test test_build_qss_themes_native_control_backgrounds asserts both QPushButton (→ elev) and
  QStatusBar (→ surface) carry a themed background for all three themes.
  - test_theme_and_icons.py: 29 passed; full suite: 1117 passed / 22 skipped.
  - ruff check clean on edited files.
  - PITFALLS updated with the "global foreground needs paired background" follow-up.

  Still uncommitted on update-ui; old attempt preserved on backup/restart-snapshot. Ready to commit as a clean feature
  commit whenever you'd like.

❯ /codex:adversarial-review @190343.png I think widgets still not applied with dark theme and not readable

● Codex Adversarial Review

  Target: working tree diff
  Verdict: needs-attention

  No-ship: the dark theme still leaks into dialogs without theming their backgrounds, producing the unreadable print UI
  shown in 190343.png.

  Findings:
  - [high] Global dark foreground is applied to dialogs whose backgrounds remain native light (view/theme.py:120-162)
  build_qss() sets a global QWidget foreground and dark-themed control rules, but only gives a background to QMainWindow
  and selected widgets. Parent-applied QSS can therefore reach modal dialogs such as the print dialog while the QDialog
  surface stays native light. The supplied screenshot shows this exact failure: labels become near-white on a light
  dialog, while combo boxes are dark islands, making print settings hard to read and use.
  Recommendation: Either scope the global foreground/control rules to the main window/object-named app surfaces, or
  explicitly theme QDialog and dialog child surfaces under every theme, including disabled labels/inputs and spin boxes.
  Add a regression test or visual check that opens the print dialog under ink-porcelain and verifies readable
  foreground/background contrast.

  The review is complete — output returned verbatim above. Per the review-only constraint of this command, I've made no
  changes.

  The finding is a real design issue: my blanket QWidget { color: fg } propagates the dark foreground into QDialogs
  (print, etc.), but those dialogs never get a themed background, so you get near-white labels on a native light surface
  (with dark-island combo boxes) — exactly what 190343.png shows.

  If you'd like me to fix it, the cleaner of the two recommended approaches is to give QDialog (and its child controls)
  a themed background so foreground and background stay paired — adding QDialog { background: bg; color: fg; } plus
  QSpinBox/QCheckBox/disabled-state rules, with a regression test that opens the print dialog under ink-porcelain. Just
  say the word and I'll implement it.

❯ execute surgical fixes

● I'll fix this surgically: pair the dark foreground with a themed background on QDialog and its child controls so
  dialogs stop being light-with-white-text. First, let me see which widgets the print dialog (and siblings) actually
  use, so I theme the right ones.

● Confirmed flaky — it passes in isolation. It's a load-sensitive heartbeat/stall timing test in the print subprocess
  runner; my changes touched only the build_qss string, the theme test file, and docs — nothing in the print/subprocess
  path. Not a regression.

  Fix complete

  The finding: my blanket QWidget { color: fg } propagated the dark foreground into child QDialogs, but QMainWindow {
  background } doesn't apply to a QDialog, so dialogs (print, export, OCR, …) stayed native-light → near-white labels on
  a light surface with dark-island combos (exactly 190343.png).

  Surgical additions to view/theme.py build_qss() — pair background with foreground for the dialog surface and its
  children:

  - QDialog { background: bg; color: fg } — the core fix.
  - QSpinBox/QDoubleSpinBox/QTextEdit/QPlainTextEdit folded into the themed input rule (elev bg, fg text, line border).
  - QComboBox QAbstractItemView — the dropdown popup (was a native light list).
  - QMenu / QMenu::item:selected — context/app menus.
  - :disabled text → fg_subtle — dims instead of vanishing.

  Verification

  prove the surface paints dark (RGB < 90) — a concrete contrast check, not just string presence.
  - test_theme_and_icons.py: 33 passed; full suite 1120 passed, 22 skipped, 1 unrelated flaky (passes in isolation).
  - ruff check clean.
  - PITFALLS updated ("Follow-up 2 — dialogs").

  Still uncommitted on update-ui; old attempt preserved on backup/restart-snapshot. Ready to commit whenever you'd like.

❯ /codex:review

● Codex Review

  Target: working tree diff

  The theme implementation misses existing parentless context menus, so part of the UI remains unthemed under the new
  dark theme. The rest of the reviewed changes appear consistent with the tests that could be run.

  Review comment:

  - [P2] Parent pop-up menus before relying on themed QSS — C:\Users\jiang\Documents\python
  programs\pdf_editor\view\pdf_view.py:309-309
  When ink-porcelain is selected, this stylesheet only cascades to widgets parented under PDFView; the existing
  thumbnail and canvas context menus are created as top-level QMenu() instances, so the new QMenu rules in build_qss()
  never reach them and those popups stay native/light. Either parent those menus to the view or apply the theme QSS at
  the application level.
