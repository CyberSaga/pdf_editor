1m=3.28ft
1m2=10.76ft2
K值5.6=80、K值11.2=160

用公制：
pipe schedule：CNS4626/SI
K值：5.6(公制) = 80(英制)(系統用英制)
撒水密度：
等級 | 密度(L)
---
嚴重危險 | 12.2
普通危險 | 8.1
輕度危險 | 4.1

KY的英制沒差，公制卻是用秒來算
常數 Constanc 4.1 在公制 = 0.9566
很小的非整數


● Now I have a complete picture of all changes. Here's the review:

  ---
  Code Review: Week 2 Acrobat-Parity Sprint (Codex Implementation)

  Overview

  Codex implemented all 4 planned steps: size: float fix, edit_text() phase extraction, error toasts, and undo/redo
  state feedback. 36 focused tests pass, 61 broader tests pass. The structural approach matches the plan well.

  ---
  Step 1: size: int → size: float — PASS

  - EditTextRequest.size, MoveTextRequest.size, edit_text() signature, EditTextCommand._size all updated correctly
  - The int(size) cast in _convert_text_to_html was correctly preserved (plan said "do NOT change this cast")
  - Test fixture updated to size=12.5 with assertion — good

  Step 2: Phase Extraction — PASS with issues

  Good:
  - Clean 3-way split: _resolve_edit_target(), _apply_redact_insert(), _verify_rebuild_edit()
  - _EditTextResolveResult frozen dataclass carries state cleanly between phases
  - edit_text() is now a thin orchestrator as planned
  - Snapshot + try/except rollback wraps all three phases correctly
  - resolve_result None-safety in except blocks (len(resolve_result.overlap_cluster) if resolve_result else 0)

  Issues:

  1. Bug in edit_commands.py — self.result assigned in wrong place:

      def undo(self) -> None:
          """撤銷操作，還原至 execute() 前的狀態。"""
          ...

          self.result: EditTextResult = EditTextResult.SUCCESS  # <-- THIS IS INSIDE undo()!

      @property
      def description(self) -> str:

  The self.result initialization is placed inside the undo() abstract method body instead of __init__ or the class body
  of EditCommand. This means EditTextCommand.result is only set on execute() (which assigns it), but the base class
  never initializes it. If any other EditCommand subclass tries to access .result before execute(), it'll
  AttributeError. This is a real bug introduced by Codex — the indentation is wrong.

  2. EditCommand.execute() return type changed to bool but SnapshotCommand.execute() and AddTextboxCommand.execute()
  don't return anything (return None). The CommandManager.execute() checks if executed is False: — this works because
  None is not False, but it's fragile. The abstract contract says -> bool but only EditTextCommand honors it.
  3. Comments stripped during extraction. The original edit_text() had detailed inline comments explaining why each
  strategy exists (e.g., "apply_redactions 必須在 insert_htmlbox 之前", "spare_height 直接告訴我們文字實際佔用高度").
  These are now gone from the extracted helpers. The plan said "structural only — no behavioral changes" but didn't say
  to strip the engineering context.

  Step 3: Error Toasts — PASS

  - EditTextResult enum is clean: SUCCESS | NO_CHANGE | TARGET_BLOCK_NOT_FOUND | TARGET_SPAN_NOT_FOUND
  - Controller correctly maps results to Chinese messages matching the design plan
  - Toast visual: muted red rgba(180,40,40,220) for errors, 2500ms duration — matches design spec exactly
  - _show_edit_result_feedback has graceful fallback to show_error() dialog if toast unavailable
  - No toast for NO_CHANGE — correct per plan ("silent is correct for no-ops")
  - Cross-page move now checks source delete result — good hardening
  - Test coverage: error toast test mocks EditTextCommand to return TARGET_BLOCK_NOT_FOUND, asserts toast called +
  show_page not called

  Minor: The error message for TARGET_SPAN_NOT_FOUND says "找不到要編輯的文字內容" but the plan specified
  "無法定位目標文字". Not a bug, but a deviation from spec.

  Step 4: Undo/Redo State Feedback — PASS

  - _set_undo_redo_action_state() updates all 4 action variants (left + right toolbar)
  - _refresh_undo_redo_action_state() correctly prefers local editor state when inline editor is active — this was the
  hardest edge case in the plan
  - update_undo_redo_enabled() stores global state, then delegates to refresh
  - _update_undo_redo_tooltips() now propagates enabled state alongside tooltips
  - Editor open: connects undoAvailable/redoAvailable signals to refresh
  - Editor close: calls refresh to restore global state
  - Initial state: disabled on startup — correct (Acrobat behavior)
  - Test: verifies local editor state overrides global when editor is open

  ---
  Acrobat-Level UX Assessment

  ┌───────────────────────────────┬────────┬─────────────────────────────────────────────────┐
  │            UX Goal            │ Status │                       Gap                       │
  ├───────────────────────────────┼────────┼─────────────────────────────────────────────────┤
  │ No silent edit failures       │ Done   │ Error copy slightly different from spec         │
  ├───────────────────────────────┼────────┼─────────────────────────────────────────────────┤
  │ Undo/redo visual state        │ Done   │ Transitions editor-local ↔ global cleanly       │
  ├───────────────────────────────┼────────┼─────────────────────────────────────────────────┤
  │ Font size fidelity            │ Done   │ float types throughout                          │
  ├───────────────────────────────┼────────┼─────────────────────────────────────────────────┤
  │ Edit pipeline maintainability │ Done   │ Clean 3-way split                               │
  ├───────────────────────────────┼────────┼─────────────────────────────────────────────────┤
  │ Error toast visual language   │ Done   │ Matches design spec (color, duration, position) │
  └───────────────────────────────┴────────┴─────────────────────────────────────────────────┘

  Critical Fix Needed

  edit_commands.py — the self.result initialization is indented inside undo() instead of being in EditCommand.__init__
  or EditTextCommand.__init__. This needs to be moved. Want me to fix it?

  Verdict

  Solid implementation that delivers all 4 planned items with correct test coverage. The one real bug is the misplaced
  self.result initialization in the base class. The comment stripping in extracted helpers is a minor loss of
  engineering context but acceptable. Overall: ready to land after the self.result indentation fix.