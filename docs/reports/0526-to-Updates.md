這是一份整理好、可以直接貼給 AI Agent 的審查結果與修復指引。我將你的批註分類為「未通過（需繼續修復/修改）」**、**「部分成功（需優化）」**與**「已通過」，並把重複的項目（如旋轉鈕問題）進行了整合，方便 Agent 快速抓到重點繼續上工：

---

## 🛠️ AI Agent 專案審查結果與後續任務

請根據以下審查結果，針對**未通過**與**部分成功**的項目查明根本成因、提出建議做法並進行實作修復。

### ❌ 一、 未通過（請立即查明成因並修復）

#### 1. 文字框選邏輯錯誤

* **現況：** 只要一拉動就會直接框選整行。
* **預期行為：** 最小單位必須是**字元**。框選文字要以「行」為單位，非框選「游標經過的文字」，而是框選游標經過的「整行」，並以游標起始位置和停止位置作為框選範圍的前後限。

#### 2. 列印功能異常

* **頁面方向未自動轉向：** 橫向版面仍以直向印出，導致長邊被縮成寬度。
* **紙張大小未依來源選擇：** A3 版面仍用 A4 紙張印出，導致畫面被縮小。

#### 3. macOS 選單列未完全遷移

* **現況：** 只有部分功能被放到了 macOS 上方列選單中。請確認是否為正確設計。

---

### ⚠️ 二、 部分成功（請依優化建議調整實作）

#### 1. 圖片與物件旋轉/縮放機制 (重點優化)

* **自由旋轉問題：** 旋轉鈕應設計為「拖曳自由轉動」，而非點選旋轉 90°。
* **旋轉互動異常：** 圖片實際轉動方向與滑鼠移動方向不一致；且旋轉鈕與圖片框在旋轉時沒有跟著轉動。
* **狀態丟失：** 轉斜的圖片一旦發生位移，就會被重新強制轉正。
* **縮放與長寬比邏輯反轉：** 目前縮放只會改圖片框，圖片長寬比被寫死。請修改為：**單純拖曳時為任意變形**，**按住 `Shift` 鍵拖曳時才是鎖定長寬比**放大縮小。

#### 2. 原有 PDF 圖片物件的操作

* **現況：** 測試發現 `01_報告書.pdf` 內的圖片可以點選與任意變形；但 `Awareness.pdf` 內的圖片完全無法點選。
* **新插入圖片邏輯不一致：** 插入新的圖片 `電子文.png` 後，該圖片卻被鎖定長寬比，與上述任意變形邏輯衝突。
* **預期行為：** 必須確保所有 PDF 內原本存在的圖片物件都能被點選操作，且行為需與全新插入的圖片保持一致。

#### 3. 效能優化 (工作流程加速)

* **現況：** 開啟檔案換頁、最佳化檔案速度有提升，但**列印送出後卡住電腦的時間**依然挺慢（送到印表機的過程太久）。請繼續優化列印排程與資料傳輸效率。

#### 4. PDF Standards 符合性確認

* **現況：** 使用者無法自行判斷。
* **後續任務：** 請 Agent 提出判斷/測試依據，證明此項目確實有符合 `docs\pdf_standards.txt` 內的規範。

---

### ✅ 三、 已通過（無需修改）

* [x] 編輯轉向文字時，文字框內文字會跟著轉向。
* [x] 文件檢視清晰度已提升（解決文字與圖片模糊問題）。
* [x] OCR 啟動前的異常等待時間已修復（解決 CPU/GPU 空轉問題）。
* [x] 頁面拉正功能 OK。
* [x] XREF 表修復功能 OK。