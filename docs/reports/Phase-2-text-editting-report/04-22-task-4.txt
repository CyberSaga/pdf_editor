● Task 4 complete — view-layer acceptance already satisfied

  Changes applied:

  - test_scripts/test_text_editing_gui_regressions.py: one-line fixture patch (view._autopan_active = False in
  _make_view). The autopan merge (bb7754f) added state that the test helper didn't mirror — 3 drag tests were failing
  with AttributeError, not a production bug. Unblocks drag-flow verification that Task 4's acceptance relies on.

  Why no production code changes were needed:

  I audited each plan-listed item and found them already covered:

  Plan item: Editor stays transparent + scene mask hides PDF text
  Status: ✅ Lock-in
  Evidence: test_phase2_refresh_mask_keeps_editor_stylesheet_transparent +
    test_build_text_editor_stylesheet_keeps_editor_background_transparent green. _build_text_editor_stylesheet at
    pdf_view.py:2057 already emits background: transparent.
  ────────────────────────────────────────
  Plan item: Mask refresh on create
  Status: ✅
  Evidence: text_editing.py:449 — create_text_editor calls refresh_text_editor_mask_color(). Covered by
    test_create_text_editor_adds_mask_item_to_hide_display_text.
  ────────────────────────────────────────
  Plan item: Mask refresh on drag
  Status: ✅
  Evidence: pdf_view.py:2856 — drag move hook. Now verified green by test_drag_move_refreshes_editor_mask_color (after
    fixture fix).
  ────────────────────────────────────────
  Plan item: Mask cleanup on finalize
  Status: ✅
  Evidence: text_editing.py:599 — _clear_text_editor_mask_item() in finalize. Covered by
    test_finalize_text_edit_removes_mask_item.
  ────────────────────────────────────────
  Plan item: Mask refresh on zoom/scale relayout
  Status: ✅ By architecture
  Evidence: _apply_scale at pdf_view.py:4653 uses QGraphicsView.setTransform — the mask (a scene-coord rect item) scales

    with the view automatically. Full rebuilds via display_page finalize the editor first (pdf_view.py:2282-2283), so
    there's no "mid-edit zoom leaves stale mask" scenario.
  ────────────────────────────────────────
  Plan item: Proxy layout uses floats for _render_scale/source-rect/rotation; no oversized/clipped rotated editors
  Status: ✅ Fixed in Task 2
  Evidence: _compute_editor_proxy_layout uses float(scaled_rect.x0)/scaled_rect.height with int(round(...)) only at the
    final pixel boundary. The int-truncation that cascaded into clipping came from _editing_initial_size =
    int(round(font_size)) at text_editing.py:433 — fixed in Task 2 to float(font_size).
    test_phase2_create_text_editor_records_fractional_initial_size green confirms.
  ────────────────────────────────────────
  Plan item: Float-tolerant no-op detection on finalize
  Status: ✅ Fixed in Task 2
  Evidence: size_changed = abs(float(session.current_size) - float(session.initial_size)) > 1e-3 at text_editing.py:564.

  Final test status: 91 passed, 2 pre-existing test_context_menu_* failures (unrelated to Phase 2 — they're browse-mode
  context menu fixtures, not text-editing pipeline).

  Ready for Task 5 (docs + final verification). This is the last task; one more batch and Phase 2 is closed.