# Optional dependencies.
# Install with: pip install -r optional-requirements.txt
#
# Note: the secured Pillow + numpy image-processing floors now live in
# requirements.txt -- they back core editor features, so calling them "optional"
# was misleading. The OCR feature (surya-ocr) is a separate, mutually-exclusive
# extra in ocr-requirements.txt because it transitively pins pillow<11
# (incompatible with the secured Pillow floor) and pulls a transformers with
# upstream-blocked CVEs. See TODOS.md "Security dependency hygiene" for the matrix.

# Native print backends for enhanced platform integration.
# Core printing still works via the Qt raster bridge without these.
pywin32>=306; platform_system == "Windows"
pycups>=2.0.4; platform_system == "Linux" or platform_system == "Darwin"

# Test runner (only needed when running test_scripts/*pytest*).
pytest>=7.0
